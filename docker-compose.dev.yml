services:
  app:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./applications.db:/app/applications.db
      - .:/app
    command: flask --app app run --host=0.0.0.0 --port=5000 --debug
